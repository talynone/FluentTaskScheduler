<ContentDialog
    x:Class="FluentTaskScheduler.Dialogs.OnboardingDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    CornerRadius="16"
    RequestedTheme="Dark">

    <ContentDialog.Resources>
        <x:Double x:Key="ContentDialogMaxWidth">600</x:Double>
        <x:Double x:Key="ContentDialogMinWidth">480</x:Double>
        <CornerRadius x:Key="OverlayCornerRadius">16</CornerRadius>
    </ContentDialog.Resources>

    <StackPanel Spacing="0" MaxWidth="560">

        <!-- Illustration area -->
        <Border x:Name="IllustrationBorder"
                Height="140"
                CornerRadius="12"
                Margin="0,0,0,24"
                Background="{ThemeResource AccentFillColorDefaultBrush}">
            <FontIcon x:Name="StepIcon"
                      Glyph="&#xE8A1;"
                      FontSize="52"
                      Foreground="White"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"/>
        </Border>

        <!-- Step title -->
        <TextBlock x:Name="StepTitle"
                   FontSize="20"
                   FontWeight="SemiBold"
                   TextWrapping="Wrap"
                   Margin="0,0,0,10"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"/>

        <!-- Step body -->
        <TextBlock x:Name="StepBody"
                   FontSize="14"
                   TextWrapping="Wrap"
                   Opacity="0.8"
                   LineHeight="22"
                   HorizontalAlignment="Center"
                   TextAlignment="Center"
                   Margin="0,0,0,20"/>

        <!-- Step hint (last slide only) -->
        <Border x:Name="HintBorder"
                Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                CornerRadius="8"
                Padding="12,8"
                Margin="0,0,0,20"
                Visibility="Collapsed">
            <StackPanel Orientation="Horizontal" Spacing="8" HorizontalAlignment="Center">
                <FontIcon Glyph="&#xE713;" FontSize="14" Opacity="0.7" VerticalAlignment="Center"/>
                <TextBlock Text="You can replay this walkthrough anytime from Settings."
                           FontSize="12"
                           Opacity="0.7"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Border>

        <!-- Step dots -->
        <StackPanel x:Name="DotsPanel"
                    Orientation="Horizontal"
                    HorizontalAlignment="Center"
                    Spacing="8"
                    Margin="0,0,0,24">
            <!-- Dots generated in code-behind -->
        </StackPanel>

        <!-- Navigation buttons -->
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button x:Name="BackButton"
                    Grid.Column="0"
                    Content="Back"
                    Click="BackButton_Click"
                    Visibility="Collapsed"
                    MinWidth="90"/>

            <Button x:Name="NextButton"
                    Grid.Column="2"
                    Content="Next"
                    Click="NextButton_Click"
                    Style="{StaticResource AccentButtonStyle}"
                    MinWidth="110"/>
        </Grid>

    </StackPanel>
</ContentDialog>
